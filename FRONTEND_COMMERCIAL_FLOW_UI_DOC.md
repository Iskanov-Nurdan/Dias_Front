# FRONTEND COMMERCIAL FLOW UI DOC

## 1) Контур и структура экранов

Документ фиксирует, как коммерческий контур должен отображаться во фронте.  
Формат статуса:
- `РЕАЛИЗОВАНО` — экран/логика есть в текущем фронте.
- `К РЕАЛИЗАЦИИ` — экран/логика описаны как целевая UI-спецификация следующего этапа.

### 1.1 Вкладки коммерческого контура

1. `Заявки` — `К РЕАЛИЗАЦИИ`
2. `Продажи` — `РЕАЛИЗОВАНО` (`/sales`)
3. `Оплаты` — `К РЕАЛИЗАЦИИ`
4. `Возвраты` — `К РЕАЛИЗАЦИИ`
5. `Брак / переработка` — частично `РЕАЛИЗОВАНО` (через `/otk` и `/warehouse`), отдельная вкладка — `К РЕАЛИЗАЦИИ`

### 1.2 Связанные экраны

- `Клиенты` (`/clients`) — `РЕАЛИЗОВАНО`
- `Склад` (`/warehouse`) — `РЕАЛИЗОВАНО`
- `ОТК` (`/otk`) — `РЕАЛИЗОВАНО`

---

## 2) Детализация по вкладкам и экранам

## 2.1 Вкладка `Заявки` (`К РЕАЛИЗАЦИИ`)

### Цель
- Регистрация и сопровождение клиентских заявок до этапа отгрузки и оплаты.

### Кто видит
- Пользователи с доступами коммерческого блока (`sales`, `clients`, `shipments`), роль менеджера/руководителя продаж.

### Данные (список)
- Номер заявки (`request_no`)
- Дата создания
- Клиент
- Номенклатура/позиции
- Заказанное количество
- Отгружено (факт)
- Остаток к отгрузке
- Сумма заявки
- Оплачено
- Долг
- Ответственный менеджер
- Статус заявки

### Фильтры
- Период
- Статус заявки (`draft`, `confirmed`, `partially_shipped`, `shipped`, `closed`, `cancelled`)
- Клиент
- Менеджер
- Наличие долга (`есть/нет`)

### Поиск
- По номеру заявки
- По названию клиента
- По телефону клиента

### Таблица
- Тип: фиксированная таблица с кликабельной строкой (переход в карточку заявки).
- Последняя колонка: меню действий.

### Колонки
- `Дата`
- `Заявка`
- `Клиент`
- `Позиции`
- `Кол-во`
- `Отгружено`
- `Сумма`
- `Оплачено`
- `Долг`
- `Статус`
- `Действия`

### Кнопки
- `Создать заявку`
- `Фильтры`
- `Сбросить фильтры`
- в строке: `Открыть`, `Отгрузить`, `Оплата`, `Возврат`, `Документы`

### Действия
- Создать/редактировать заявку
- Перевести в подтвержденную
- Провести частичную/полную отгрузку
- Зарегистрировать предоплату/доплату
- Оформить возврат
- Печать документов

### Статусы
- `Черновик`
- `Подтверждена`
- `Частично отгружена`
- `Отгружена`
- `Закрыта`
- `Отменена`

### Итоги (верхняя панель)
- Кол-во активных заявок
- Сумма активных заявок
- Сумма дебиторки
- Сумма предоплат

---

## 2.2 Вкладка `Продажи` (`РЕАЛИЗОВАНО`, `/sales`)

### Цель
- Фиксация факта продажи с выбором складской партии, пересчетом количества и печатью накладной.

### Кто видит
- Пользователи с доступом `sales`.

### Данные (список)
- Продажа (ID)
- Дата продажи
- Клиент
- Партия склада/продукт
- Качество партии (`Годный`/`Брак`)
- Количество (в штуках или упаковках + пересчет в штуки)
- Выручка
- Себестоимость
- Прибыль
- Комментарий

### Фильтры
- В текущем UI отдельной панели фильтров нет.
- Фактическая фильтрация на экране не реализована (выводится список `sales/`).

### Поиск
- В текущем UI поля поиска нет.

### Таблица
- Колонки:
  - `Дата`
  - `Клиент`
  - `Партия`
  - `Количество`
  - `Выручка`
  - `Себестоимость`
  - `Прибыль`
  - `Действия`
- Клик по строке открывает модалку `Накладная`.

### Кнопки
- `Создать`
- В меню строки:
  - `Редактировать`
  - `Накладная`
  - `Удалить`

### Действия
- Создание продажи
- Редактирование продажи
- Удаление продажи
- Просмотр накладной
- Скачивание PDF/черновика накладной

### Статусы
- Явного статуса продажи на экране нет.
- Косвенные признаки:
  - качество партии: `Годный`/`Брак`
  - форма списания: `Не упаковано` / `Упаковано` / `Открытая упаковка`

### Итоги
- Блок итогов (агрегаты по продажам) в текущем UI отсутствует.

---

## 2.3 Вкладка `Оплаты` (`К РЕАЛИЗАЦИИ`)

### Цель
- Учет поступлений от клиента: предоплата, доплата, полная оплата, возврат оплаты.

### Кто видит
- Пользователи с доступом коммерческого контура (`sales`/`shipments`).

### Данные
- Номер платежа
- Дата/время
- Клиент
- Привязка к заявке/продаже
- Тип платежа (`Предоплата`, `Доплата`, `Полная оплата`, `Возврат оплаты`)
- Сумма
- Способ оплаты (`Наличные`, `Перевод`, `Карта`, `Иное`)
- Комментарий
- Статус платежа

### Фильтры
- Период
- Клиент
- Тип платежа
- Статус (`pending`, `confirmed`, `cancelled`)
- Способ оплаты

### Поиск
- По номеру платежа
- По клиенту
- По номеру заявки/продажи

### Таблица/колонки
- `Дата`
- `Платеж`
- `Клиент`
- `Связанный документ`
- `Тип`
- `Сумма`
- `Способ`
- `Статус`
- `Действия`

### Кнопки/действия
- `Добавить оплату`
- `Подтвердить`
- `Отменить`
- `Открыть документ`

### Статусы
- `Черновик`
- `Подтвержден`
- `Отменен`

### Итоги
- Получено сегодня
- Получено за период
- Сумма предоплат
- Сумма доплат
- Долг клиентов

---

## 2.4 Вкладка `Возвраты` (`К РЕАЛИЗАЦИИ`)

### Цель
- Оформление возвратов после продажи с маршрутизацией: на склад годного, в брак, на переделку.

### Кто видит
- Коммерция + склад + ОТК (по роли).

### Данные
- Номер возврата
- Дата
- Клиент
- Основание (продажа/заявка)
- Товар/партия
- Количество
- Причина возврата
- Направление возврата (`на склад`, `в брак`, `на переделку`)
- Статус обработки

### Фильтры/поиск
- Период
- Клиент
- Направление возврата
- Статус
- Поиск по номеру возврата/продажи/клиента

### Таблица/колонки
- `Дата`
- `Возврат`
- `Клиент`
- `Основание`
- `Товар`
- `Кол-во`
- `Направление`
- `Статус`
- `Действия`

### Кнопки/действия
- `Создать возврат`
- `Подтвердить прием`
- `Передать в ОТК`
- `Передать на переделку`
- `Закрыть возврат`

### Статусы
- `Создан`
- `Принят на складе`
- `Передан в ОТК`
- `Передан на переделку`
- `Закрыт`
- `Отменен`

### Итоги
- Кол-во возвратов за период
- Возвраты на склад
- Возвраты в брак
- Возвраты на переделку

---

## 2.5 Вкладка `Брак / переработка`

### Текущее покрытие (`РЕАЛИЗОВАНО`)
- `ОТК` (`/otk`) — вкладки `Ожидают`, `История`.
- `Склад` (`/warehouse`) — качество партии `Годный`/`Брак`, резерв, упаковка.

### Цель
- Управление полным циклом дефектной продукции: фиксация брака, маршрутизация, продажа брака, списание, завершение переделки.

### Кто видит
- ОТК, склад, коммерция, руководитель.

### Данные/фильтры/поиск/таблица
- В текущем UI разделены между `ОТК` и `Склад`.
- Отдельная единая вкладка `Брак / переработка` с общим журналом маршрутов — `К РЕАЛИЗАЦИИ`.

### Обязательные статусы в едином экране (целевая модель)
- `Брак выявлен`
- `На складе брака`
- `Передан на переделку`
- `Переделка завершена`
- `Продан как брак`
- `Списан`

---

## 3) Формы и модалки: место, поля, валидации, кнопки, endpoint, payload

## 3.1 `Продажа` (модалка, `/sales`) — `РЕАЛИЗОВАНО`

### Где открывается
- Кнопка `Создать` на странице продаж.
- Действие `Редактировать` в строке таблицы.

### Поля
- `Дата продажи` (`date`)
- `Клиент` (`Select`, допускается "Без клиента")
- `Партия *` (`Select`)
- `Количество *` (штуки или упаковки)
- `Штук в упаковке *` (для режима упаковок)
- `Цена`
- `Комментарий`

### Обязательные
- `Партия`
- Количество > 0
- Для `Упаковок`: `Упаковок >= 1`, `Штук в упаковке >= 1`

### Валидации
- Нельзя сохранить, если:
  - количество <= 0
  - превышен остаток партии
  - не найден `catalog product` для выбранной партии
  - ошибка конвертации количества
- При `saleUnit=package` рассчитывается `sold_pieces` из `packages * pieces_per_package`.

### Кнопки
- `Отмена`
- `Сохранить`
- после создания: экран `Готово` с кнопками `Закрыть` и `Накладная`

### Endpoint
- Создание: `POST sales/`
- Редактирование: `PATCH sales/{id}/`

### Payload (фактическая отправка)
- `client_id`, `client`
- `product` (catalog product id)
- `warehouse_batch`, `warehouse_batch_id`
- `sale_mode` (`pieces` | `packages`)
- `sold_pieces`
- `sold_packages` (когда упаковки)
- `length_per_piece` (если доступно)
- `quantity`
- `quantity_unit` (`piece` | `package`)
- `quantity_input`
- `stock_form`
- `piece_pick` (`from_sealed_package` | `from_open_package`, по форме хранения)
- `override_pieces_per_package` (если задано)
- `price`
- `comment`
- `sale_date`, `date`

---

## 3.2 `Накладная` (модалка, `/sales`) — `РЕАЛИЗОВАНО`

### Где открывается
- Клик по строке продажи.
- Действие `Накладная` в меню строки.

### Отображаемые поля
- Дата
- Клиент
- Партия (включая бейдж `Брак`, если применимо)
- Количество
- Выручка
- Себестоимость
- Прибыль
- Комментарий

### Кнопки
- `Закрыть`
- `Скачать PDF`

### Endpoint
- Попытка скачивания по очереди:
  - `GET sales/{id}/nakladnaya/`
  - `GET sales/{id}/waybill/`
  - `GET sales/{id}/invoice/`

### Поведение при отсутствии endpoint
- Генерация локального `HTML`-черновика накладной и скачивание файла `nakladnaya-{id}-draft.html`.

---

## 3.3 `Удалить продажу` (ConfirmModal, `/sales`) — `РЕАЛИЗОВАНО`

### Где открывается
- Меню строки продажи -> `Удалить`.

### Поля
- Без полей ввода (текст подтверждения).

### Кнопки
- `Удалить`
- `Отмена`

### Endpoint
- `DELETE sales/{id}/`

### Payload
- Нет (только path-параметр `id`).

---

## 3.4 `Клиент` (модалка создания/редактирования, `/clients`) — `РЕАЛИЗОВАНО`

### Где открывается
- Кнопка `Создать` на странице клиентов.
- Клик по строке клиента (редактирование).

### Поля
- `Название / компания *`
- `Контактное лицо`
- `Телефон`
- `WhatsApp / Telegram`
- `Email`
- `Клиент активен` (checkbox)
- Блок `Ещё`:
  - `Доп. телефон`
  - `Адрес`
  - `Тип`
  - `Комментарий`

### Обязательные
- Только `Название / компания`.

### Валидации
- HTML-валидация email (`type="email"`).
- Тримминг строк перед отправкой.

### Кнопки
- `Отмена`
- `Сохранить`

### Endpoint
- Создание: `POST clients/`
- Редактирование: `PATCH clients/{id}/`

### Payload
- `name`
- `phone`
- `contact_person`
- `messenger`
- `whatsapp_telegram`
- `email`
- `phone_alt`
- `address`
- `client_type`
- `notes`
- `comment`
- `is_active`

---

## 3.5 `История клиента` (модалка, `/clients`) — `РЕАЛИЗОВАНО`

### Где открывается
- Меню строки клиента -> `История`.

### Данные
- Таблица `Дата`, `Событие`.
- Внутри события: тип + описание/комментарий.

### Endpoint
- Основной: `GET clients/{id}/history/`
- Fallback: `GET sales/?page_size=200&client_id={id}`

### Payload
- Нет (GET).

---

## 3.6 `Удалить клиента` (ConfirmModal, `/clients`) — `РЕАЛИЗОВАНО`

### Где открывается
- Меню строки клиента -> `Удалить` (показывается только если у клиента нет продаж).

### Endpoint
- `DELETE clients/{id}/`

### Payload
- Нет.

---

## 3.7 `Резерв` (модалка, `/warehouse`) — `РЕАЛИЗОВАНО`

### Где открывается
- Меню строки складской партии -> `Резерв`.

### Поля
- `Продукт` (readonly)
- `Количество *`
- `№ продажи` (опционально, в `Дополнительно`)

### Валидации
- `Количество >= 1`
- Только для партий со статусом `available`.

### Кнопки
- `Отмена`
- `Зарезервировать`

### Endpoint
- `POST warehouse/batches/reserve/`

### Payload
- `batch_id`
- `quantity`
- `sale_id` (опционально)

---

## 3.8 `Упаковать` (модалка, `/warehouse`) — `РЕАЛИЗОВАНО`

### Где открывается
- Кнопка `Упаковать` на странице склада.

### Поля
- `Партия *` (доступные `inventory_form=unpacked`, `status=available`)
- `Упаковок *`
- `Штук в каждой *`
- `Комментарий`

### Обязательные
- Партия
- Упаковок >= 1
- Штук в упаковке >= 1

### Валидации
- Проверка достаточности остатка: `packages_count * pieces_per_package <= available_qty`.

### Кнопки
- `Отмена`
- `Упаковать`

### Endpoint
- `POST warehouse/batches/package/`

### Payload
- `warehouse_batch_id`
- `warehouse_batch`
- `pieces_per_package`
- `packages_count`
- `comment` (опционально)

---

## 3.9 `Проверка партии` (модалка ОТК, `/otk`) — `РЕАЛИЗОВАНО`

### Где открывается
- Вкладка `Ожидают` -> кнопка `Проверить`.

### Поля
- `Принято, шт`
- `Брак, шт`
- `Комментарий`
- `Причина брака` (обязательна, если `Брак > 0`)
- `Инспектор`
- `Дата проверки`

### Валидации
- `Принято + Брак > 0`
- Если известен выпуск партии: `Принято + Брак == Выпуск`
- Если `Брак > 0`, обязательна `Причина брака`.

### Кнопки
- `Отмена`
- `Сохранить`

### Endpoint
- `POST batches/{id}/otk_accept/`

### Payload
- `otk_accepted` (строка)
- `otk_defect` (строка)
- `accepted`
- `rejected`
- `otk_status` (`accepted` или `rejected`)
- `otk_defect_reason` (опционально)
- `otk_comment` (опционально)
- `otk_inspector` (опционально)
- `otk_inspector_name` (опционально)
- `otk_checked_at` (опционально)

---

## 4) Сценарии (пошагово в UI)

## 4.1 Создание заявки (`К РЕАЛИЗАЦИИ`)
- Вкладка `Заявки` -> `Создать заявку`.
- Заполнить клиента, позиции, количество, цену, комментарий.
- Нажать `Сохранить`.
- Ожидаемый результат: строка в таблице со статусом `Подтверждена` или `Черновик`.

## 4.2 Частичная отгрузка (`К РЕАЛИЗАЦИИ`)
- В карточке заявки нажать `Отгрузить`.
- Выбрать партию, указать количество меньше остатка.
- Подтвердить отгрузку.
- Ожидаемый результат: статус `Частично отгружена`, поле `Отгружено` обновлено.

## 4.3 Полная отгрузка (`РЕАЛИЗОВАНО` через `/sales`, для заявки — `К РЕАЛИЗАЦИИ`)
- `/sales` -> `Создать`.
- Выбрать партию и количество в пределах остатка (или `Макс.`).
- Сохранить.
- Ожидаемый результат: продажа создана, открывается состояние `Готово`, доступна `Накладная`.

## 4.4 Предоплата (`К РЕАЛИЗАЦИИ`)
- Вкладка `Оплаты` -> `Добавить оплату`.
- Тип: `Предоплата`.
- Указать заявку/клиента/сумму/способ.
- Подтвердить.

## 4.5 Доплата (`К РЕАЛИЗАЦИИ`)
- Вкладка `Оплаты` -> `Добавить оплату`.
- Тип: `Доплата`.
- Привязать к заявке/продаже с неполной оплатой.
- Подтвердить.

## 4.6 Возврат на склад (`К РЕАЛИЗАЦИИ`)
- `Возвраты` -> `Создать возврат`.
- Направление: `На склад`.
- Подтвердить прием.
- Ожидаемый результат: партия в `warehouse` с качеством `Годный`.

## 4.7 Возврат в брак (`ЧАСТИЧНО РЕАЛИЗОВАНО`)
- `Возвраты` (будущая вкладка) или модалка маршрутизации -> `В брак`.
- В текущем UI брак виден в `/warehouse` (качество `Брак`) и `/otk` истории.

## 4.8 Возврат на переделку (`К РЕАЛИЗАЦИИ`)
- Оформить возврат с направлением `На переделку`.
- Передать в производственный/ОТК поток с отдельным статусом.

## 4.9 Продажа брака (`РЕАЛИЗОВАНО`)
- `/sales` -> `Создать`.
- Выбрать партию с качеством `Брак` (помечена в селекте и таблице).
- Сохранить продажу.
- Накладная отображает бейдж `Брак`.

## 4.10 Списание брака (`К РЕАЛИЗАЦИИ`)
- Вкладка `Брак / переработка` -> действие `Списать`.
- Указать количество и причину списания.
- Подтвердить.

## 4.11 Завершение переделки (`К РЕАЛИЗАЦИИ`)
- Вкладка `Брак / переработка` -> карточка переделки -> `Завершить`.
- Указать выход: годный/остаточный брак.
- Подтвердить.

---

## 5) Отдельные блоки

## 5.1 Карточка клиента (`РЕАЛИЗОВАНО`)
- Реализована в модалке `Клиент` на `/clients`.
- Показывает и редактирует:
  - основные контакты,
  - активность клиента (`Клиент активен`),
  - расширенные реквизиты в секции `Ещё`.
- Правило UI: неактивный клиент не должен попадать в выбор новых продаж (`/sales` уже фильтрует активных).

## 5.2 История клиента (`РЕАЛИЗОВАНО`)
- Модалка `История: {имя клиента}`.
- Источник 1: `clients/{id}/history/`.
- Источник 2 (fallback): продажи клиента.
- Формат строки: дата + событие (тип, описание).

## 5.3 Документы на печать (`РЕАЛИЗОВАНО`)
- Документ: `Накладная` из `/sales`.
- Кнопка `Скачать PDF`.
- Логика:
  - сначала попытка получить бинарный документ с API,
  - если API не готов/неверный ответ — скачать локальный HTML-черновик.

## 5.4 WebSocket / refetch логика (`РЕАЛИЗОВАНО`)
- Канал: `/ws/operational/?token=...`.
- События:
  - `connected` (служебное),
  - `change` (триггер обновления).
- `useOperationalRefetch(resources, refetch)` подписывает экран на `resource`.
- Экраны:
  - `/sales`: слушает `sale`, `warehouse_batch` и перезагружает продажи + справочники.
  - `/warehouse`: слушает `warehouse_batch`, `production_batch`, `batch`.
  - `/otk`: слушает `production_batch`, `batch`, `warehouse_batch`, `recipe_run`.
- При закрытии WS кодом `4001` отправляется событие `dias-operational-ws-token-rejected`.

## 5.5 Статусы и их отображение в UI

### Склад (`/warehouse`)
- `available` -> `Доступно`
- `reserved` -> `Зарезервировано`
- `shipped`/`sold` -> `Продано`

### Качество склада
- `good` -> бейдж `Годный`
- `defect` -> бейдж `Брак` + причина (`defect_reason`)

### Форма хранения
- `unpacked` -> `Не упаковано`
- `packed` -> `Упаковано`
- `open_package` -> `Открытая упаковка`

### ОТК (`/otk`)
- `pending/awaiting` -> `Ожидает проверки`
- `accepted` + `defect=0` -> `Годный → склад`
- `accepted` + `accepted>0` + `defect>0` -> `Годн. + брак → склад`
- `rejected/defect` -> `Брак → склад`

### Клиенты (`/clients`)
- `is_active=true` -> `Активен`
- `is_active=false` -> `Неактивен`

---

## 6) Точное соответствие “что фронт должен показать”

Для следующего этапа обязательны:
- Добавить отдельные вкладки `Заявки`, `Оплаты`, `Возвраты`, `Брак / переработка` в навигацию коммерческого контура.
- На каждой вкладке реализовать:
  - поиск,
  - фильтры,
  - таблицу с фиксированными колонками,
  - меню действий в строке,
  - верхние итоги (агрегаты).
- Все формы должны повторять паттерн текущего UI:
  - `ConfirmModal` при закрытии грязной формы,
  - inline-валидации до отправки,
  - серверная ошибка в `modal__error`,
  - toast при успешном сохранении.
- Все действия, влияющие на состояние склада/продаж/ОТК, должны быть подписаны на `useOperationalRefetch` по нужным `resource`.

Этот документ — рабочая UI-спецификация: текущий реализованный функционал уже отражен, отсутствующие вкладки и сценарии даны в точной форме для реализации без дополнительных уточнений по структуре экранов.
