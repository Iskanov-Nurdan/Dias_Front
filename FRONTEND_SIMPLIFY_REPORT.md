# Что удалено

## Тексты (абзацы, подсказки, леды)

- **MaterialsPage.jsx**: абзац про остаток и FIFO после прихода; абзац про блокировку единицы измерения; блок `materials-history__hint` про списание и FIFO; префикс «Сырьё:» в пополнении — осталось имя материала одной строкой; подпись «Сумма партии:» — осталось число и «сом».
- **PlasticProfilesPage.jsx**: абзац `plastic-profiles-modal__hint` после сохранения профиля.
- **UsersPage.jsx**: блок **«Ещё фильтры»** (`Collapse`); мобильная связка **«Фильтры»** + `FiltersModal` с отдельным экраном фильтров.

## Блоки / состояние UI

- Скрытый второй ряд фильтров пользователей и модальное окно фильтров на мобильной вкладке «Пользователи».

---

# Что изменено

## Фильтры

- **UsersPage**: один `FilterBar` с `variant="row"` и объединённым списком полей (`USERS_FILTERS_ALL`: поиск, роль, статус, сортировка). На узком экране — горизонтальный скролл контейнера (`users-page__filters--row` в **UsersPage.scss**).

## Toolbar

- Без отдельной кнопки «Фильтры» для пользователей: фильтры всегда в той же зоне списка.

## Таблицы

- Без изменений в этом коммите (ранее: рецепты, химия, производство — primary + меню в других файлах по задаче упрощения).

## Прочее

- **MaterialsPage**: подсказка по заблокированной единице — `title` на `Select` вместо абзаца.
- **OTKPage.jsx**: удалён `useEffect` с вызовом несуществующего `setOtkFiltersOpen` (после отказа от модалки фильтров).

---

# Что стало лучше

- Меньше текста на складе сырья и в модалке профиля ПВХ.
- Фильтры пользователей видны сразу, без раскрытия и без отдельного модального шага на телефоне.
- Сборка проходит без ошибки ESLint по OTK.

---

# Состояние по другим страницам (проверка кода)

- Импортов `FiltersModal` в `src/features/**` нет — скрытые модальные фильтры на страницах фич не используются.
- **OTKPage**, **WarehousePage**, **RecipesPage**, **ProductionPage**, **ChemistryPage**, **SalesPage**, **PlasticProfilesPage**: длинные леды под заголовками и часть модальных «цепочек» убраны ранее в рамках той же задачи; строки таблиц с primary + `ActionMenu` где настроено в этих файлах.
